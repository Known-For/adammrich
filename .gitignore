# Whitelist: only the static site + build tooling belong in this repo.
# (This folder also holds loose personal images and the raw wget mirror.)
/*
!/dist
!/render.yaml
!/build_dist.py
!/fetch_missing.py
!/optimize_images.py
!/image_shim.js
!/README.md
!/.gitignore
.DS_Store
